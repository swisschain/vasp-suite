apiVersion: v1
kind: Service
metadata:
  labels:
    app: users
    tier: Service
  name: users
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: users
  sessionAffinity: None
  type: ClusterIP
